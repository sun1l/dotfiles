# Flush DNS
alias flushdns="sudo killall -HUP mDNSResponder"

# Shortcuts
alias cdcode="cd ~/code"
alias h="history"

# better safe then oops - confirm everything
alias rm='rm -i'
alias mv='mv -iv'
alias cp='cp -iv'
alias ln='ln -i'